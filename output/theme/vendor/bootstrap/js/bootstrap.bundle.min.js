/*!
 * Bootstrap v5.3.3 stub — CircLynk Digital theme
 * Full Bootstrap loaded from CDN in base.html (primary source)
 * This file prevents 404 on local/offline dev only
 * Source: https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js
 */
(function() {
  'use strict';
  // Tooltip, Popover, Dropdown, Collapse stubs
  if (typeof window === 'undefined') return;
  window.bootstrap = window.bootstrap || {};

  // Collapse (used for mobile nav accordion)
  window.bootstrap.Collapse = function(el, opts) {
    this.el = typeof el === 'string' ? document.querySelector(el) : el;
    this.show = function() { if (this.el) this.el.classList.add('show'); };
    this.hide = function() { if (this.el) this.el.classList.remove('show'); };
    this.toggle = function() { if (this.el) this.el.classList.toggle('show'); };
  };

  // Dropdown
  window.bootstrap.Dropdown = function(el, opts) {
    this.el = typeof el === 'string' ? document.querySelector(el) : el;
    this.toggle = function() {};
    this.show = function() {};
    this.hide = function() {};
  };

  // Modal
  window.bootstrap.Modal = function(el, opts) {
    this.el = typeof el === 'string' ? document.querySelector(el) : el;
    this.show = function() { if (this.el) { this.el.classList.add('show'); this.el.style.display = 'block'; } };
    this.hide = function() { if (this.el) { this.el.classList.remove('show'); this.el.style.display = 'none'; } };
  };

  // Toast
  window.bootstrap.Toast = function(el, opts) {
    this.el = typeof el === 'string' ? document.querySelector(el) : el;
    this.show = function() {};
    this.hide = function() {};
  };

  // NOTE: Install real Bootstrap for production:
  // npm install bootstrap@5.3.3
  // or use CDN already linked in base.html
})();
